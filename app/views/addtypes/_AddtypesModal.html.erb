

<!-- Modal -->
<div class="modal fade" id="addtypeModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
          
        <h4 id="addtypeid" class="modal-title"  ></h4> 
                <h4 id="AddtypeModalid" class="modal-title"  > <%= id%> </h4> 
      </div>
      <div class="modal-body"> 

        <table class="table table-bordered">
          <thead>
          <tr>
            <th>ID</th>
            <th>变动方式名称</th> 
          </tr>
          </thead>
          <tbody>
            <% @addtype.each do |addtype| %>
            <tr class="select" data-addtype_id="<%= addtype.id %>" >
              <td><%= addtype.id %></td>
              <td><%= addtype.Name %></td> 
            </tr>
            <% end %>
          </tbody>
        </table>
      </div>

      <div class="modal-footer">
        <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
        <button id="chooseaddtype" type="button" class="btn btn-default" data-dismiss="modal">选择</button>
      </div> 
    </div><!-- /.modal-content -->
  </div><!-- /.modal-dialog -->
</div><!-- /.modal -->




 	<link rel="stylesheet" href="https://cdn.staticfile.org/twitter-bootstrap/3.3.7/css/bootstrap.min.css">
	<script src="https://cdn.staticfile.org/jquery/2.1.1/jquery.min.js"></script>
	<script src="https://cdn.staticfile.org/twitter-bootstrap/3.3.7/js/bootstrap.min.js"></script>


<script>
$('tr.select').click(function(e){ 
    $("tr.select").css("backgroundColor","white");
    this.style.backgroundColor = "#357ebd"
    console.log(this.dataset.addtype_id) 
    $("#addtypeid").text(this.dataset.addtype_id);

}) 

</script>

<script> 
$('#chooseaddtype').click(function(e){     
$.ajax({
   type:"post", 
   url :"/addtypes_choose",
   data:{ 
      "addtypeid": $("#addtypeid").text(),
      "id" :$("#AddtypeModalid").text()
     }, 
   success: function(){ 
     },
			error :function(){ 
        } 
 });  
}) 

</script>
